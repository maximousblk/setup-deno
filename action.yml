name: 'Setup Deno'
description: 'GitHub Actions to install and set up Deno'
author: 'Maximous Black'
branding:
  icon: 'box'
  color: 'gray-dark'
inputs:
  version:
    required: false
    description: 'Deno version'
runs:
  using: 'node12'
  main: 'dist/index.js'
